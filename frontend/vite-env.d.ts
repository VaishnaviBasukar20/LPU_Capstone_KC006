// FIX: The original line '/// <reference types="vite/client" />' was removed.
// This resolves a "Cannot find type definition file" error. This is a safe
// workaround because the project doesn't use Vite-specific client-side types.
